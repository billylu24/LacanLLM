# Legacy LacanLLM implementation

This directory contains the complete pre-v0.3 project state: scripts, datasets,
adapters, experiment logs, outputs, environment notes, and the previous README.

The artifacts are preserved for comparison and provenance. They are not inputs
to new training runs except where an explicit, hashed phase-1 data configuration
references them. New implementation work lives at the repository root.

